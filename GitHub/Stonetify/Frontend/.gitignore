# ----------------------------------------------------------------------
# 1. 프로젝트 종속성 및 빌드 관련 파일 (Expo, Node.js)
# ----------------------------------------------------------------------
# Dependency directories
node_modules/

# Expo files
.expo/
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----------------------------------------------------------------------
# 2. IDE 및 로컬 설정 파일 (VSCode 등)
# ----------------------------------------------------------------------
.vscode/

# ----------------------------------------------------------------------
# 3. 비정상적으로 포함된 시스템 및 사용자 환경 파일 (스크린샷에서 확인된 내용)
#    (이 파일들은 Git이 Stonetify 폴더 밖을 보고 있다는 의미이므로,
#     업로드되지 않도록 확실하게 무시해야 합니다.)
# ----------------------------------------------------------------------
# OS 및 사용자 환경 파일 무시
/../
/../.bash_history
/../.gitconfig
/../.ssh
/../AppData/
/../Contacts/
/../Documents/
/../Downloads/
/../Music/
/../Videos/
/../NTUSER.DAT*
/../ntuser.ini

# 권한 문제로 경고가 발생했던 폴더도 추가 (내부에 중요한 파일이 없다면)
Templates/
시청 메뉴/